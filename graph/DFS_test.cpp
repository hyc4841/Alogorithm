#include <iostream>
#include <vector>
#include <unordered_set>

using namespace std;
// 그래프는 인접 리스트로 구현
vector<char, vector<char>> adjList;
vector<char> route; // 방문 경로
unordered_set<char> visited;




vector<char> solution(vector<pair<char, char>> graph, char start) {
    // graph에는 부모 - 자식 관계의 문자가 pair 형태로 존재함.


}

int main() {

    return 0;
}